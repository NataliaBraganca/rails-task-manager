<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<h2><%= @task.completed %></h2>

<p><%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></p>


<p><%= link_to("Back to all your tasks", tasks_path ) %></p>
